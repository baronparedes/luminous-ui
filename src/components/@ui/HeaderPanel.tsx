import styled from 'styled-components';

const HeaderPanel = styled.div`
  background-color: var(--nav-bg);
  box-shadow: var(--nav-shadow) 0px 0px 5px 0px;
  padding: 0;
  margin: 0;
  margin-bottom: 2em;
`;

export default HeaderPanel;
